# web-ready film material: placeholders from scripts/generate-clips.sh, or the
# real clips and voice overs built by scripts/prepare-media.sh
frontend/public/clips/
frontend/public/audio/

# raw delivery from the film team, unpacked from handoff-magnus.zip - ~1 GB of
# masters that prepare-media.sh reads and never needs to be in the repo
resources/handoff/

# scanned post-it artwork, ~90 MB of PNGs - kept out of the repo like the
# clips and supplied out of band
frontend/public/post-its/
resources/post-its.zip

node_modules/
dist/
.env
