# Copy to .env and adjust. Never commit .env.
PROJECT_NAME=interactive-movie

# Host port for the production stack (compose.yml).
WEB_PORT=8080

# Host port for the dev stack (compose.dev.yml). Bound to loopback only.
DEV_PORT=5173
